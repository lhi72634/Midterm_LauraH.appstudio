//Laura Higueras
/*
let myAnimals = ["dog", "cat", "horse", "meerkat"]

for (i = 0; i < myAnimals.length;i++) {
    console.log(`${myAnimals[i]}`)
}

let newAnimal = prompt("Enter a new animal.")
    newAnimal = newAnimal.toLowerCase()
    newAnimals = myAnimals.sort()
    let newAnimalsReverse = myAnimals.reverse
    myAnimals.push(newAnimal)

console.log(myAnimals[myAnimals.length - 1])

for (i = 0; i < myAnimals.length;i++) {
    console.log(`The last animal is a/an ${myAnimals[myAnimals.length - 1]}`)
}
*/