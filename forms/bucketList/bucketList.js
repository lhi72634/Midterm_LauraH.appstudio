/*
let bucketList = []
let i = 0

let doneWithList = false
while (doneWithList == false){
    bucketList[i] = prompt("Enter item into Bucket List: ")
    i++
    doneWithList = confirm("Are you done with the Bucket List?")
}
for (i = 0; i < bucketList.length;i++) {
    console.log(`${bucketList[i]}`)
}
*/